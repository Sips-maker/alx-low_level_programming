#include <limits.h>
#include <stdio.h>
#include "holberton.h"

/**l
 * main - Entry point
 *
 * Return: Always 0
 */

int main(void)
{
   char * my_pointer;
   my_pointer = "Siphiwe";
    printf("%s is %s\n", "My name", my_pointer);

    return 0;
}
